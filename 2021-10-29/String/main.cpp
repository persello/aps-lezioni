#include <iostream>
#include <string>

using namespace std;

int main() {

  // strcpy -> s = p
  // strcmp -> s < p, s > p, s == p, s != p
  // strcat -> s = s + p, s += p, s = s + ch, s += ch
  // strlen -> s.size(), s.length()

  string s, p("pluto");
  s = "pippo";
}
